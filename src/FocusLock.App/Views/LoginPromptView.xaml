<UserControl x:Class="FocusLock.App.Views.LoginPromptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="450">
    <Grid Background="#F8FAFC">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="420" Padding="32" CornerRadius="0"
                Background="#FFFFFF" BorderBrush="#0F172A" BorderThickness="2">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.1" Color="#000000"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center" Width="356">
                <!-- App Logo Icon -->
                <Image Source="pack://application:,,,/Assets/Logo.png" Width="64" Height="64" HorizontalAlignment="Center" Margin="0,0,0,16" RenderOptions.BitmapScalingMode="HighQuality"/>

                <TextBlock Text="FlyLock Browser" FontSize="22" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="Google Student Authentication" FontSize="13" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center" Margin="0,0,0,12"/>

                <TextBlock Text="Your default system web browser has been launched to authenticate your institutional Google account." 
                           TextWrapping="Wrap" TextAlignment="Center" FontSize="12" Foreground="#475569" LineHeight="18" Margin="0,0,0,20"/>

                <!-- Mandatory Domain Badge -->
                <Border Background="#ECFDF5" BorderBrush="#10B981" BorderThickness="1.5" Padding="10,8" Margin="0,0,0,20">
                    <TextBlock Text="🔒 Mandatory Domain: @bitsathy.ac.in" FontSize="11" FontWeight="Bold" Foreground="#047857" HorizontalAlignment="Center"/>
                </Border>

                <!-- Primary Action Button: Launch System Browser -->
                <Button x:Name="BtnOpenBrowser" Click="BtnOpenBrowser_Click" Height="46" Margin="0,0,0,12" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#0F172A"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0" Padding="0" BorderBrush="#0F172A" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2563EB"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="SIGN IN WITH GOOGLE IN BROWSER" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Secondary Manual Check Button -->
                <Button x:Name="BtnCheckLogin" Click="BtnCheckLogin_Click" Height="38" Margin="0,0,0,16" Cursor="Hand" Background="Transparent" Foreground="#0F172A" BorderBrush="#0F172A" BorderThickness="1.5" FontWeight="Bold" FontSize="12" Content="I HAVE LOGGED IN →"/>

                <!-- Dual-Layer Spinning Loading Animation Sequence -->
                <Grid Width="60" Height="60" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Image Source="pack://application:,,,/Assets/Loading_Centre.png" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <Image Source="pack://application:,,,/Assets/Loading_Circle.png" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" RenderTransformOrigin="0.5,0.5">
                        <Image.RenderTransform>
                            <RotateTransform x:Name="LoginSpinTransform" Angle="0"/>
                        </Image.RenderTransform>
                        <Image.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(Image.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="360" Duration="0:0:1.2"
                                                         RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Image.Triggers>
                    </Image>
                </Grid>

                <!-- Status & Spinner Text -->
                <TextBlock x:Name="TxtStatus" Text="Waiting for Google verification in browser..." FontSize="11" FontWeight="SemiBold" Foreground="#64748B" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
