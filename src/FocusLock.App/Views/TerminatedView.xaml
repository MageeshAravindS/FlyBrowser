<UserControl x:Class="FocusLock.App.Views.TerminatedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid Background="#FFFFFF">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="560" Padding="40" CornerRadius="0"
                Background="#FFFFFF" BorderBrush="#DC2626" BorderThickness="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.15" Color="#DC2626"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center">
                <Border Width="64" Height="64" CornerRadius="0" Background="#DC2626" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock Text="🛑" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                </Border>

                <TextBlock Text="Exam Session Terminated" FontSize="26" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,0,0,12"/>

                <TextBlock x:Name="TxtReason" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="#334155" LineHeight="22" Margin="0,0,0,24">
                    Your exam session was terminated because the application window lost focus multiple times beyond the permitted limit.
                </TextBlock>

                <!-- Session Reference ID Box -->
                <Border Background="#F1F5F9" CornerRadius="0" BorderBrush="#0F172A" BorderThickness="1" Padding="16" Margin="0,0,0,24">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="SESSION REFERENCE ID" FontSize="11" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock x:Name="TxtSessionId" Text="SESSION-ID-0000" FontSize="16" FontFamily="Consolas" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Please inform your exam proctor or administrator to reset your machine." FontSize="13" Foreground="#475569" TextAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock x:Name="TxtProctorHint" Text="Proctors: Press ESC to exit" FontSize="12" FontWeight="Bold" Foreground="#0F172A" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
