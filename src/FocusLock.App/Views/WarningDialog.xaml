<Window x:Class="FocusLock.App.Views.WarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Focus Violation Warning" Height="340" Width="520"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False" Topmost="True">
    <Grid>
        <Border Background="#FFFFFF" BorderBrush="#DC2626" BorderThickness="3" CornerRadius="0" Padding="32">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.2" Color="#DC2626"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <Border Width="56" Height="56" CornerRadius="0" Background="#DC2626" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <TextBlock Text="⚠️" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="FOCUS VIOLATION DETECTED" FontSize="20" FontWeight="Bold" Foreground="#991B1B" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                
                <TextBlock FontSize="13" Foreground="#475569" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,24">
                    Focus loss recorded (<Run x:Name="RunCount" Text="1" FontWeight="Bold" Foreground="#DC2626"/> of <Run x:Name="RunMax" Text="3" FontWeight="Bold"/>).<LineBreak/>
                    Repeated violations will result in automatic exam session termination.
                </TextBlock>

                <Button x:Name="BtnDismiss" Click="BtnDismiss_Click" Content="RETURN TO EXAM" Height="46" Width="260" HorizontalAlignment="Center" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#DC2626"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0" BorderBrush="#991B1B" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#B91C1C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
