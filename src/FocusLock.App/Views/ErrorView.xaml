<UserControl x:Class="FocusLock.App.Views.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid Background="#FFFFFF">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="520" Padding="40" CornerRadius="0"
                Background="#FFFFFF" BorderBrush="#DC2626" BorderThickness="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.15" Color="#DC2626"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center">
                <Border Width="64" Height="64" CornerRadius="0" Background="#DC2626" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock Text="⚠️" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                </Border>

                <TextBlock Text="System Error Encountered" FontSize="24" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock x:Name="TxtErrorDetails" TextWrapping="Wrap" TextAlignment="Center" FontSize="13" Foreground="#334155" LineHeight="20" Margin="0,0,0,24">
                    An unrecoverable system error occurred while running the exam session.
                </TextBlock>

                <Button x:Name="BtnExit" Click="BtnExit_Click" Content="EXIT APPLICATION" Height="48" Width="240" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#DC2626"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0" BorderBrush="#991B1B" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#B91C1C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
