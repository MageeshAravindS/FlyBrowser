<UserControl x:Class="FocusLock.App.Views.CompletionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid Background="#FFFFFF">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="500" Padding="40" CornerRadius="0"
                Background="#FFFFFF" BorderBrush="#16A34A" BorderThickness="3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="4" Opacity="0.15" Color="#16A34A"/>
            </Border.Effect>

            <StackPanel HorizontalAlignment="Center">
                <Border Width="64" Height="64" CornerRadius="0" Background="#16A34A" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock Text="✅" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                </Border>

                <TextBlock Text="Assessment Completed" FontSize="26" FontWeight="Bold" Foreground="#16A34A" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Your assessment response has been submitted successfully. Thank you for using FlyLock Browser." 
                           TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="#334155" LineHeight="22" Margin="0,0,0,28"/>

                <Button x:Name="BtnExit" Click="BtnExit_Click" Content="EXIT BROWSER" Height="48" Width="240" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#16A34A"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="0" BorderBrush="#15803D" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#15803D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
